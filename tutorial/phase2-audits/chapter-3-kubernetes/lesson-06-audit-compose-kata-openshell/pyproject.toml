[project]
name = "lesson-06-audit-compose-kata-openshell"
version = "0.1.0"
description = "Sandboxing tutorial — lesson 2.3.6: auditing OpenShell over Kata; the host sensor goes blind and the L7 trail survives."
requires-python = ">=3.12"
# Empty on purpose: the suite runs inside the sandbox pod, and this leaf only orchestrates the
# openshell CLI, kubectl and Tetragon, parsing their output with the standard library.
dependencies = []

[tool.ruff]
# Count the dirs: leaf -> chapter -> phase -> tutorial -> repo root.
extend = "../../../../ruff.toml"
src = ["."]
