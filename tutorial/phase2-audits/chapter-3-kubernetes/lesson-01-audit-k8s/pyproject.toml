[project]
name = "lesson-01-audit-k8s"
version = "0.1.0"
description = "Sandboxing tutorial — lesson 2.3.1: auditing the Kubernetes rung with host Tetragon and the apiserver audit log."
requires-python = ">=3.12"
# Empty on purpose: the suite runs inside the pod, and this leaf only orchestrates kubectl +
# Tetragon and parses their output with the standard library.
dependencies = []

[tool.ruff]
# Count the dirs: leaf -> chapter -> phase -> tutorial -> repo root.
extend = "../../../../ruff.toml"
src = ["."]
