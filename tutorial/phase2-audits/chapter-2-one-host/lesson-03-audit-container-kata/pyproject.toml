[project]
name = "lesson-03-audit-container-kata"
version = "0.1.0"
description = "Sandboxing tutorial — lesson 2.2.3: auditing the Kata rung; the host sensor goes fully blind, the sensor moves into the guest."
requires-python = ">=3.12"
dependencies = []

[tool.ruff]
extend = "../../../../ruff.toml"
src = ["."]
