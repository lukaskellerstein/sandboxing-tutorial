[project]
name = "lesson-01-audit-container"
version = "0.1.0"
description = "Sandboxing tutorial — lesson 2.2.1: auditing the container rung with host Tetragon."
requires-python = ">=3.12"
# Empty on purpose: the suite runs inside the agent image, and this leaf only orchestrates podman +
# Tetragon and parses their output with the standard library.
dependencies = []

[tool.ruff]
# Count the dirs: leaf -> chapter -> phase -> tutorial -> repo root.
extend = "../../../../ruff.toml"
src = ["."]
